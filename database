REATE DATABASE ksk_events;
USE ksk_events;

CREATE TABLE admin (
    username VARCHAR(50),
    password VARCHAR(50)
);

INSERT INTO admin VALUES ('admin', 'ksk123');

CREATE TABLE events (
    id INT AUTO_INCREMENT PRIMARY KEY,
    event_name VARCHAR(100),
    event_date VARCHAR(50),
    venue VARCHAR(100),
    description TEXT
);
